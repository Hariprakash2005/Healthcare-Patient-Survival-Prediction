Model artifacts (`model.pkl`, `scaler.pkl`, `feature_names.npy`) are generated automatically after running:

```
python -m src.train_model
```

Ensure Python and all dependencies in `requirements.txt` are installed before executing the training pipeline.

